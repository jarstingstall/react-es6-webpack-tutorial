# react-es6-webpack-tutorial

A port of the React Docs Tutorial to ES6 using Webpack.

```bash
$ npm install
$ webpack
$ node server.js
```

The app is served at [http://localhost:3000](http://localhost:3000).
